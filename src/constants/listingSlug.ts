export const SLUG_MAX_LENGTH = 60;

export const SLUG_FALLBACK = 'propiedad';

export const SHORT_ID_LENGTH = 8;

export const REF_MAX_LENGTH = 100;

export const COMBINING_MARKS_PATTERN = /[̀-ͯ]/g;

export const NON_SLUG_PATTERN = /[^a-z0-9]+/g;

export const SLUG_REF_PATTERN = /^(?:[a-z0-9]+(?:-[a-z0-9]+)*-)?([0-9a-f]{8})$/;
